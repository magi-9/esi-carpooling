version: "3.9"

# Include individual service compose files
include:
  - backend/api-gateway-service/docker-compose.yml
  - backend/payment-service/docker-compose.yml
  - backend/ride-discovery-service/docker-compose.yml
  - frontend/docker-compose.yml

# Shared network for all services
networks:
  esi-network:
    driver: bridge
    name: esi-network
